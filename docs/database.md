# Database

The PostgreSQL contract is in `Backend/prisma/contract.prisma`. Generated contract metadata and the seed are colocated in `Backend/prisma`. Use `npm run db:init` and `npm run db:seed` from the repository root.