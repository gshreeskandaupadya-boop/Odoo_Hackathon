# Deployment

Deploy the `Frontend/` Next.js package to Vercel with `DATABASE_URL`, `NEXTAUTH_URL`, and `NEXTAUTH_SECRET` configured. Use a hosted PostgreSQL database; Vercel cannot access a local database URL.