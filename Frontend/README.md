# DealFlow360 Frontend

Next.js 16 frontend and route boundary for DealFlow360.

```powershell
npm install
npm run dev
```

The application routes live in `src/app`. Reusable UI and feature page implementations live in `src/components`. Backend APIs are exposed through `src/app/api` and call the sibling Backend package.
